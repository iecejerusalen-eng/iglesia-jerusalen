import SearchPalette from '../public/SearchPalette';

/**
 * Unified Command & Search Palette.
 * Re-exports the unified SearchPalette to ensure all global triggers
 * and legacy references invoke the exact same smart search modal.
 */
export default SearchPalette;
