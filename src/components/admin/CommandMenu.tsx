/**
 * CommandMenu legado: Ahora unificado con el SearchPalette universal montado en App.tsx.
 * Este componente se conserva como un adaptador para evitar dependencias rotas.
 */
export default function CommandMenu() {
  return null;
}
